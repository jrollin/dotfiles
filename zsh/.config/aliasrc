
# use nvim
alias vim="nvim"
alias vi="nvim"
alias oldvim="vim"
alias python="python3"

alias kubectl="minikube kubectl --"
alias k="kubectl"

alias jr="cd $PROJECTS_DIR/julienrollin/"

alias pr="cd $PROJECTS_DIR/"
alias dot="cd $DOTFILES_DIR/"

# fuzzy search 
alias my="cd \$(find $PROJECTS_DIR $DOTFILES_DIR $DATA_DIR -mindepth 1 -maxdepth 1 -type d | fzf)"
